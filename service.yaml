apiVersion: v1
kind: Service
metadata:
  name: scaterbrain-musicstore-service
spec:
  selector:
    app: scaterbrain-musicstore
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
  type: {{ .Values.service.type }}
